#ifndef MACRO_H
#define MACRO_H

/**
 * File: 3-function_like_macro
 * @X: input
 * Desc: Header file that defines a macro to compute the abs value of a number.
 */

#define ABS(X) ((X) < 0 ? -(X) : (X))

#endif /* MACRO_H */

